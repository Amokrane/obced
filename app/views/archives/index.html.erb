<% @archives.each do |archive| %>
	<%= "#{archive.title}, by #{archive.user.nickname}" %> <br/>
	<%= sanitize Kramdown::Document.new(archive.content).to_html %> <br/>
	<%= "#{archive.score} points" %> <br/>
	<%= archive.created_at %> <br/> <br/> <br/>
<% end %>
