<% @archives.each do |archive| %>
	<%= archive.created_at %>
	<%= ": #{archive.title} (#{archive.score} points)" %> <br/>
	<%= "by #{archive.user.nickname}" %> <br/>
	<%=  "Description: #{archive.description}" %> <br/>
	 <br/> <br/> 
<% end %>
